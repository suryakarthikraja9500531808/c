#include <stdio.h>
#include<conio.h>
void main();
{
int n,digit,sum=0;
scanf("%d",&n);
             if(n%2==0)
             {
             	printf("%d",n);
             }
             else
             {
             	printf("%d",n-1);
             }
	getch();
  }
